import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export const metadata = {
  title: "Workflow Builder | Atna",
  description: "Design and configure verification pipelines.",
};

export default function WorkflowsPage() {
  return (
    <PlaceholderPage
      title="Workflow Builder"
      description="Design and configure verification pipelines. Templates and custom workflows will appear here."
    />
  );
}
