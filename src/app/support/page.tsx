import { SupportPage } from "@/components/support/SupportPage";

export const metadata = {
  title: "Support | Atna",
  description: "Manage support queries, explore guides, and share feedback.",
};

export default function SupportRoute() {
  return <SupportPage />;
}
