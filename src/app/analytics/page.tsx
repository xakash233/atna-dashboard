import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function AnalyticsPage() {
  return (
    <PlaceholderPage
      title="Analytics"
      description="Analytics module shell. No data in content source — awaiting API integration."
    />
  );
}
