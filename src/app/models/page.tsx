import { PlaceholderPage } from "@/components/ui/PlaceholderPage";

export default function ModelsPage() {
  return (
    <PlaceholderPage
      title="Models"
      description="Models module shell. No data in content source — awaiting API integration."
    />
  );
}
